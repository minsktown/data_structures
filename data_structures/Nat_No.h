#pragma once

/*
* 객체: 0에서 시작하여 INT_MAX 까지의 순서화된 정수
*/

int zero();
bool is_zero(int x);
int add(int x, int y);
int sub(int x, int y);
bool equal(int x, int y);
int successor(int x);
bool is_greater(int x, int y);